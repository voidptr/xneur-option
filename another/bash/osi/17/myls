# Автор: Янкин Н.В. МК-201
# Задание N17

ls -l | grep "^-" | sed "s/^-\(...\)[^ ]*[ ]*[^ ]*[ ]*[^ ]*[ ]*[^ ]*[ ]*\([0-9]*\)[ ]*[^ ]*[ ]*[^ ]*[ ]*[^ ]*[ ]*\([^ ]*\)/Имя файла: \3 размер: \2 права владельца: \1/" | sort -nk 5

#s -l | sort -nk 5 | awk '{if ($3!="") print ("Имя файла: "$9" размер: "$5" права владельца: "substr($1,2,3))}'
